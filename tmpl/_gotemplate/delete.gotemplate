// Delete will remove a {{.ValueType}} from the map by key
func ({{.ReceiverName}} {{if .ByReference}}*{{end}}{{.StructName}}) Delete(key {{.KeyType}}) {
{{if .ByReference}}\\\
	{{.ReceiverName}}.mx.Lock()
	defer {{.ReceiverName}}.mx.Unlock()

{{end}}\\\
{{if .LazyInstantiates}}\\\
    {{.ReceiverName}}.onceToken.Do(func() {
    	{{.ReceiverName}}.{{.InternalMapName}} = make(map[{{.KeyType}}]{{.ValueType}})
	})
{{end}}\\\
	delete({{.ReceiverName}}.{{.InternalMapName}}, key)
}
